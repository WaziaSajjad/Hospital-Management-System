// #pragma once
// #include <vector>
// #include "Patient.h"

// class PriorityQueue {
// public:
//     std::vector<Patient> patients;

//     void clear() { patients.clear(); }
//     void push_back(const Patient& p) { patients.push_back(p); }
//     auto begin() const { return patients.begin(); }
//     auto end() const { return patients.end(); }

//     // Optionally: operator[] or at()
//     const Patient& operator[](size_t index) const { return patients[index]; }
//     size_t size() const { return patients.size(); }
// };
